
module.exports ={
    drawTriangle: function(limit){
        let symbole = "";
        for (let nbr = 0; nbr < limit; nbr ++){
            symbole = symbole + "$";
            console.log(symbole)
        }
        
    }
    
}
